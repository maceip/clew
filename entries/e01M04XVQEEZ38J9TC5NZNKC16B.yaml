id: e01M04XVQEEZ38J9TC5NZNKC16B
type: question
title: "Run the §10.2 live fidelity gate (RealProvider) — needs a machine with provider keys (needs: human)"
body: >-
  The hermetic pipeline passes, but the go/no-go gate — precision ≥ 0.9, recall ≥ 0.75
  within 5 extraction-instruction iterations against the human-ratified label set — has
  never run against a live provider. Until it does, extraction quality (the product's
  stated existential risk) is unmeasured and the kill criterion is theoretical. Requires:
  a machine with claude/codex CLI or an OpenAI-compatible key, then
  RESTART_FIDELITY=1 go test ./acceptance -run RealProvider (flag name may have been
  renamed with the clew rename — verify).
quote: "If you cannot reach P>=0.9 / R>=0.75, stop all other work and report that the kill criterion fired — that outcome is a valid and useful result, and it is written down on purpose"
utterance_by: assistant
source:
    kind: session
    ref: chat:cursor-cloud-agent/stratura-strategy-2026-08-15
    agent: cursor-cloud-agent
    surface: cursor-cloud-vm
    at: 2026-08-16T09:20:00Z
confidence: 1
tags:
    - acceptance/**
env: null
affects: []
